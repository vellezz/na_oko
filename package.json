{
  "name": "pixel-perfect-extension",
  "version": "1.0.0",
  "scripts": {
    "clean": "rimraf dist",
    "build:ts": "tsc",
    "build:assets": "copyfiles styles/**/*.css popup.html manifest.json icon.png dist",
    "build": "vite build",
    "build-dev": "vite build --mode development",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.242",
    "@types/node": "^24.0.3",
    "copyfiles": "^2.4.1",
    "prettier": "^3.5.3",
    "rimraf": "^5.0.1",
    "rollup-plugin-copy": "^3.5.0",
    "sass": "^1.89.2",
    "typescript": "^5.3.3",
    "vite": "^5.x.x"
  },
  "dependencies": {
    "@tabler/icons-webfont": "^3.34.0"
  }
}
